name: PlaywriteFramework

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  Demo1:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Dependencies
        run: npm ci

      - name: Install browsers
        run: npx playwright install --with-deps

      - name: Run Cucumber tests
        run: npm test
        env:
          CI: true

      - name: Generate HTML report
        if: always()
        run: npm run report

      - name: Upload Cucumber report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cucumber-report
          path: reports/

      - name: Send Email with Report Link
        if: always()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          username: vinayakhanagi1234@gmail.com
          password: Ka26y1698@
          subject: "Playwright Automation Report - GitHub Actions"
          to: vinayakhanagi12@email.com
          from: GitHub Actions
          body: |
            Hi Team,

            Playwright + Cucumber automation execution is completed.

            Repository: ${{ github.repository }}
            Branch: ${{ github.ref_name }}
            Job Status: ${{ job.status }}

            Download the HTML report from the link below:
            https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}

            Regards,
            Automation Framework



      

  
